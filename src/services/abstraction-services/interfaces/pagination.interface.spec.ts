import * as sourceModule from './pagination.interface';

describe('pagination interface', () => {
  it('loads as an independent module boundary', () => {
    expect(sourceModule).toBeDefined();
  });
});
