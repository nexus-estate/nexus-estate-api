import * as sourceModule from './provider-account-error-codes';

describe('provider account error codes', () => {
  it('loads as an independent module boundary', () => {
    expect(sourceModule).toBeDefined();
  });
});
