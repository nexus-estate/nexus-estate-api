import * as sourceModule from './provider-registration.response';

describe('provider registration response', () => {
  it('loads as an independent module boundary', () => {
    expect(sourceModule).toBeDefined();
  });
});
