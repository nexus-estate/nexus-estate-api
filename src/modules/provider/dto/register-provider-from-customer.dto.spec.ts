import * as sourceModule from './register-provider-from-customer.dto';

describe('register provider from customer dto', () => {
  it('loads as an independent module boundary', () => {
    expect(sourceModule).toBeDefined();
  });
});
