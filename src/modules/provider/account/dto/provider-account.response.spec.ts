import * as sourceModule from './provider-account.response';

describe('provider account response', () => {
  it('loads as an independent module boundary', () => {
    expect(sourceModule).toBeDefined();
  });
});
