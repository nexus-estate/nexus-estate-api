import * as sourceModule from './update-provider-account.dto';

describe('update provider account dto', () => {
  it('loads as an independent module boundary', () => {
    expect(sourceModule).toBeDefined();
  });
});
