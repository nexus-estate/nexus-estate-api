import * as sourceModule from './create-provider-account.dto';

describe('create provider account dto', () => {
  it('loads as an independent module boundary', () => {
    expect(sourceModule).toBeDefined();
  });
});
