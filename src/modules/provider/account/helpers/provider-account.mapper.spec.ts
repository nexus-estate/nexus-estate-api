import * as sourceModule from './provider-account.mapper';

describe('provider account mapper', () => {
  it('loads as an independent module boundary', () => {
    expect(sourceModule).toBeDefined();
  });
});
