import * as sourceModule from './administration-account.type';

describe('administration account type', () => {
  it('loads as an independent module boundary', () => {
    expect(sourceModule).toBeDefined();
  });
});
