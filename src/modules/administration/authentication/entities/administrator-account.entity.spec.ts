import * as sourceModule from './administrator-account.entity';

describe('administrator account entity', () => {
  it('loads as an independent module boundary', () => {
    expect(sourceModule).toBeDefined();
  });
});
