import * as sourceModule from './admin-refresh-token.dto';

describe('admin refresh token dto', () => {
  it('loads as an independent module boundary', () => {
    expect(sourceModule).toBeDefined();
  });
});
