import * as sourceModule from './provider-registration-administration.service';

describe('provider registration administration service', () => {
  it('loads as an independent module boundary', () => {
    expect(sourceModule).toBeDefined();
  });
});
