import * as sourceModule from './provider-registration-review.response';

describe('provider registration review response', () => {
  it('loads as an independent module boundary', () => {
    expect(sourceModule).toBeDefined();
  });
});
