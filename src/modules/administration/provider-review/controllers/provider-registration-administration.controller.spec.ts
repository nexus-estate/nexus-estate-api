import * as sourceModule from './provider-registration-administration.controller';

describe('provider registration administration controller', () => {
  it('loads as an independent module boundary', () => {
    expect(sourceModule).toBeDefined();
  });
});
