import * as sourceModule from './register-customer.dto';

describe('register customer dto', () => {
  it('loads as an independent module boundary', () => {
    expect(sourceModule).toBeDefined();
  });
});
